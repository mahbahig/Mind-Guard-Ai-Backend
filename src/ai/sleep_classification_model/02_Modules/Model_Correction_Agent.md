MODULE: Model Correction & Class RebalancingObjective: Resolve the class collapse issue where the model fails to predict Wake, N1, and N3 stages.1. Data Rebalancing InstructionsThe current model is biased toward N2 and REM. You must implement the following:Calculate Class Weights: Compute weights inversely proportional to class frequencies in the training set.Apply Weights to Loss: Pass these weights into the model.compile or model.fit stage to penalize the model more heavily for missing the minority classes (Wake, N1, N3).Verify Label Encoding: Ensure that the label mapping is consistent (e.g., 0: Wake, 1: N1, 2: N2, 3: N3, 4: REM) and that the final Softmax layer has exactly 5 units.2. Feature Enhancement InstructionsStatistical features alone are failing to distinguish between stages. You must add:Delta Features: Calculate the difference between the current epoch and the previous one ($X_{t} - X_{t-1}$). Sleep is a temporal process; Wake usually follows a change in signal intensity.Frequency Domain: Extract the Spectral Power in specific bands.N3 is defined by high power in very low frequencies (Delta-like rhythms in BP/PPG).Wake has higher frequency "noise" from movement and muscle tone.Normalization: Apply RobustScaler or Per-Subject Z-scoring. Global scaling fails because different subjects have different baseline blood pressures.3. Architecture AdjustmentsSequential Context: Since you are using an LSTM/GRU, ensure you are passing sequences of epochs (e.g., a window of 5–10 epochs) rather than treating each 30s block as an independent data point.Dropout Tuning: Reduce dropout if the model is underfitting, or increase it if it's "memorizing" the N2 majority.4. Validation ProtocolConfusion Matrix Monitoring: After every epoch, generate a temporary confusion matrix. If any column remains at $0.00$, the agent must stop and adjust the class weights.Balanced Accuracy: Use balanced_accuracy_score as the primary metric for saving the "Best Model" checkpoint.